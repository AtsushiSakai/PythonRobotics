version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build_doc:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: python -V
          name: doc_build

workflows:
  main:
    jobs:
      - build_doc
